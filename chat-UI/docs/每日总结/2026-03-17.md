# 每日开发总结

## 2026-03-17

### 一、新增功能

#### 1. 场景引擎提醒推送
- 在 `scenario-service.cjs` 新增 `/api/alerts` 和 `/api/ack` API
- 支持前端轮询获取车辆状态提醒（胎压、电量）
- 在 `VoiceChatPage.tsx` 添加提醒轮询逻辑，每30秒检查一次

#### 2. 胎压偏低提醒增强
- 胎压提醒现在包含四个轮子数据 (FL/FR/RL/RR)
- 显示具体的胎压数值 (bar)
- 提供处理建议（检查扎钉、补气到2.9bar、到店检查）

#### 3. 空调控制意图修复
- 修复"关闭空调"被错误识别为"打开空调"的bug
- 优化 `vehicleControl.ts` 中的模糊匹配逻辑，正确区分开关状态

#### 4. Figma设计稿读取
- 配置FIGMA_TOKEN实现Figma API调用
- 成功读取小米YU7中控屏设计稿 (node-id: 2329-3962)
- 导出为PNG图片并解析内容

#### 5. YU7中控屏页面渲染
- 基于Figma设计稿创建 `yu7-screen.html`
- 使用Python http.server在8080端口启动
- 完整还原：导航、车速、辅助驾驶、车辆状态、胎压警告

---

### 二、技能学习

#### 1. 前端设计技能 (frontend-design)
- 安装位置: `~/.claude/skills/frontend-design`
- 内容：高质量前端UI设计指南，避免"AI slop"审美

#### 2. Canvas设计技能 (canvas-design)
- 安装位置: `~/.claude/skills/canvas-design`

#### 3. 高德地图LBS技能 (amap-lbs)
- 安装位置: `~/.claude/skills/amap-lbs`
- 配置: AMAP_WEBSERVICE_KEY
- 功能：POI搜索、路径规划、旅游规划

#### 4. 高德地图JSAPI技能 (amap-jsapi)
- 安装位置: `~/.claude/skills/amap-jsapi`
- 配置: AMAP_JSAPI_KEY

---

### 三、问题处理

#### 1. CORS跨域问题
- 场景引擎API返回hasNew=false的问题
- 修复：修改逻辑，有胎压/电量提醒时总是返回hasNew: true

#### 2. previousData初始化问题
- 初始化时间太新导致获取不到历史提醒
- 修复：初始化时间设为2020年

#### 3. 意图识别优先级
- "关闭空调"错误匹配到"空调打开"
- 修复：增加开关状态冲突检测

#### 4. API Token过期
- 小米汽车 serviceToken 过期，返回401 Unauthorized
- 状态：待用户提供新token

---

### 四、API/接口汇总

| 接口 | 地址 | 状态 |
|------|------|------|
| 小米汽车发送地址 | iccc.xiaomiev.com | ❌ Token过期 |
| Figma设计稿 | api.figma.com | ✅ 正常 |
| 高德地图POI | restapi.amap.com | ✅ 正常 |
| 场景引擎 | localhost:3001 | ✅ 正常 |

---

### 五、明日计划

- [ ] 集成高德地图POI搜索到车载助手
- [ ] 完善胎压偏低二级报警逻辑
- [ ] 更新serviceToken
- [ ] 继续完善中控屏UI
